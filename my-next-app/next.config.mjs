/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // You can add other Next.js specific configurations here if needed,
  // but the webpack block is not used by `next dev --turbo`.
};

export default nextConfig;
